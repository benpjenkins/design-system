import {
  createGlobalStyle,
  css,
  keyframes,
  isStyledComponent,
  StyleSheetManager,
  ThemeConsumer,
  ThemeProvider,
  withTheme,
} from "styled-components";

export default {
  createGlobalStyle,
  css,
  keyframes,
  isStyledComponent,
  StyleSheetManager,
  ThemeConsumer,
  ThemeProvider,
  withTheme,
};
